// ------------------------------------------------------

const INITIAL_STATE = { id: undefined }

// ------------------------------------------------------

export default function (state = INITIAL_STATE, action) {
    switch (action.type) {
        default: 
            return state
    }
}

// ------------------------------------------------------